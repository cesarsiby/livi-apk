r.post('/intercity/shipments/:id/partner-deliver', requireRoles('admin'), asyncHandler(async(req,res)=>{const b=z.object({credential:z.string().min(4).max(5000),latitude:z.number().optional(),longitude:z.number().optional()}).parse(req.body||{});const {verifyDeliveryProof}=await import('./delivery.js');const result=await verifyDeliveryProof(req.params.id,{credential:b.credential,latitude:b.latitude,longitude:b.longitude,actorRole:'admin',actorId:req.user.sub,requestId:res.locals.requestId,ip:req.ip,userAgent:req.get('user-agent')});ok(res,result)}));
